"""
Desenvolva um gerador de tabuada, capaz de gerar a 
tabuada de qualquer número inteiro entre 1 a 10. O 
usuário deve informar de qual número ele deseja 
ver a tabuada.

"""
def check(a):
    if a >=18:
        return a
def msg(s):
    return (input(f"Qual es seu {s} : "))

n = int(msg("numero"))
i=0
while i<=10:
    print(f"{n} x {i:2} = {i*n}")
    i+=1