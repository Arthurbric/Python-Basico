"""
Crie uma função que recebe dois parâmetros e faz a soma,
produto, divisão e subtração desses parâmetros após isso
implemente a aplicação dessa função em outro programa e
mostro o resultado no segundo programa

"""
def mul(a,b):
    return a*b,a/b,a+b,a-b
def msg(m):
    return int(input(f"Nota {m} : "))
n1 = msg(1)
n2 = msg(2)
mu,div,som,sub = mul(n1,n2)
print(f"\n{n1} x {n2} = {mu}")
print(f"{n1} / {n2} = {div:.2f}")
print(f"{n1} + {n2} = {som}")
print(f"{n1} - {n2} = {sub}")