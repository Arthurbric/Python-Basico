"""
Faça um programa que leia um nome de usuário e a sua senha e não
aceite a senha igual ao nome do usuário, mostrando uma mensagem
de erro e voltando a pedir as informações.

"""
def check(a):
    if a >=18:
        return a
def msg(s):
    return (input(f"Qual vai ser {s} : "))
nome = str(msg("Usuario"))

while True:
    senha = str(msg("Senha"))
    if senha == nome:
        print("A senha nao pode ser o memso do Usario\n(( Digita novamente ))")
        continue
    break
print(f"\n--> {nome:10} :Usuario\n--> {senha:10} :Senha")